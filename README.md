# edipark.github.io

Sunghyun Park's personal portfolio, built with Next.js and vinext.

## Local development

```bash
npm install
npm run dev
```

Edit the portfolio content in `app/page.tsx` and the visual system in
`app/globals.css`.
